# 隐私策略 / Privacy Policy

> Vantage 安全防护（VirusDetector Vantage 特供版）

**最后更新日期 / Last updated：2026-09-03**

---

## 概述

本扩展为 Vantage 浏览器内置的钓鱼/恶意下载防护组件，基于开源项目 VirusDetector（MIT License，原作者 酪酪肽Lolitide）改造。

本扩展**不收集、不存储、不出售**任何个人身份信息，**不进行任何用户行为追踪**。以下说明本扩展涉及的全部数据流动。

## 我们不收集的

- ❌ 不收集个人身份信息（姓名、邮箱、密码等）
- ❌ 不收集浏览历史记录
- ❌ 不收集 Cookie、表单数据、剪贴板内容
- ❌ 不嵌入任何第三方分析或广告 SDK
- ❌ 不进行用户行为追踪或画像
- ❌ 不将任何数据出售或共享给第三方商业机构

## 扩展处理的数据（安全检测必需）

本扩展仅在以下场景涉及数据流动，全部出于**安全检测这一唯一目的**，且仅传输**用户正在访问的网站的域名**（不含完整 URL、不含路径/参数/Cookie、不含任何页面内容）：

### 1. 域名注册信息查询（RDAP）

**目的**：查询当前访问网站的域名注册时间，用于评估域名可信度（新注册域名往往是钓鱼站特征）。

**传输内容**：仅当前访问网站的**域名**。

**目标服务器**：
- IANA RDAP 引导服务（`data.iana.org`）——获取 TLD → 注册局 RDAP 服务器映射
- 各顶级域注册局 RDAP 服务器（均由 IANA 引导文件指定，全 HTTPS）

**说明**：所有查询均通过 HTTPS 加密传输。查询结果仅用于本地评分，缓存 24 小时后自动清除。对于无公开 RDAP 服务的 TLD（如 `.cn`），域名年龄按"未知"中性处理，不发起额外查询。

### 2. ICP 备案核验（可选，默认开启）

**目的**：核验网站是否具备工信部 ICP 备案，识别盗用/伪造备案号的钓鱼站。

**传输内容**：仅当前访问网站的**域名**。

**目标服务器**（第三方 ICP 查询服务，全 HTTPS）：
- `uapis.cn`（主源）
- `cn.apihz.cn`（备援）

**控制权**：您可以在扩展设置 →「备案查询 API」中**随时关闭**此功能，关闭后回退为纯页面文本扫描，不影响其他检测。

### 3. 公共后缀（PSL）解析预热

**目的**：正确提取网站主域名（如 `a.b.example.co.uk` → `example.co.uk`）。

**传输内容**：域名 + 固定后缀的 PTR 查询，发往阿里公共 DNS（`dns.alidns.com`，HTTPS）。

### 4. 版本更新检查

**目的**：检查是否有新版本可用。

**传输内容**：仅 HTTP 请求头中的浏览器 User-Agent 与当前扩展版本号，不包含任何用户数据。

**目标服务器**：
- `asystech.cn/vantage/api/version`（主源）
- GitHub Releases API（`api.github.com`，回退源，仅查询公开 Release 元数据）

**频率**：安装/更新后检查一次，此后每 24 小时一次（失败后 1 小时重试）。经浏览器商店或企业策略安装的渠道会自动跳过远程检查。

### 5. 本地存储（永不离开设备）

以下数据**仅**存储在您本地的浏览器存储（`chrome.storage.local`）中，**永不离开您的设备**：

- 域名检测结果缓存（24 小时后自动清除）
- 您手动添加的白名单域名
- 您确认拦截后记录的下载域名黑名单（90 天自动清理）
- 扩展设置项
- 您在本扩展内触发的"误报/确认钓鱼"操作记录（上限 200 条，仅保存在本地）

> 说明：历史版本曾提供"用户上报"功能（将误报/钓鱼域名提交给开发者）。**该功能自 2026-09-03 起已下线**：本扩展不会向任何服务器发送您的举报内容，举报操作仅保存在本地。

## 数据共享与披露

本扩展**不与任何第三方共享数据**，以下安全检测所必需的技术场景除外：

| 第三方 | 数据类型 | 目的 | 加密 |
|--------|---------|------|------|
| IANA / 各注册局 RDAP 服务器 | 当前访问网站的域名 | 查询域名注册信息 | HTTPS |
| uapis.cn / cn.apihz.cn | 当前访问网站的域名 | ICP 备案核验（可关闭） | HTTPS |
| 阿里公共 DNS | 域名 PSL 解析 | 提取主域名 | HTTPS |

以上第三方均不受我们控制，请参阅各自的隐私政策。除上述场景外，本扩展不做任何网络传输。

## 权限说明

本扩展申请的浏览器权限及其唯一用途：

| 权限 | 用途 |
|------|------|
| `activeTab` | 读取当前标签页 URL 以进行安全检测 |
| `storage` | 本地存储检测缓存、白名单和设置 |
| `downloads` | 监听下载事件，拦截疑似恶意文件的下载 |
| `scripting` | 注入内容脚本以分析页面内容 |
| `notifications` | 对高风险网站发送桌面通知 |
| `webNavigation` | 监听页面导航事件以触发安全检测 |
| `alarms` | 定时（24h）触发版本更新检查 |

## 您的控制权

- **白名单**：任何网站均可通过弹窗一键加入白名单，完全跳过所有检测和网络请求
- **关闭第三方查询**：ICP 备案 API 可在设置页独立关闭
- **卸载即清除**：卸载扩展后，所有本地存储数据将被浏览器自动删除

## 联系我们

如有任何隐私相关问题，请通过以下方式联系：

- 🌐 官网：<https://asystech.cn/vantage>
- 🐙 开源仓库（Vantage 特供版）：<https://github.com/asystech-chen/VantageVD>
- 上游项目：<https://github.com/Lolitide/VirusDetector>

---

*本隐私策略可能随扩展功能更新而调整，重大变更将通过扩展更新说明告知。*
